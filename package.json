{
  "name": "preact-omg",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && parcel build index.html",
    "clean": "rm -rf dist",
    "start": "parcel serve index.html"
  },
  "dependencies": {
    "preact": "^8.3.1"
  },
  "devDependencies": {
    "parcel": "^1.9.7",
    "typescript": "^3.0.3"
  }
}
