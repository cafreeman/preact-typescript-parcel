# Preact and Parcel and Typescript

This repo contains the most basic parcel/preact/typescript setup I could come up with.

To start a dev server, run `yarn start`.

To build for deployment, run `yarn build`.
